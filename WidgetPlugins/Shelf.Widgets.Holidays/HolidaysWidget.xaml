<UserControl x:Class="Shelf.Widgets.Holidays.HolidaysWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Padding="12,8,12,10"
            Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="0,0,0,1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ===== Header ===== -->
            <Grid Grid.Row="0" Margin="2,0,2,6" HorizontalAlignment="Left">
                <TextBlock x:Name="TitleText"
                           Text="{DynamicResource Holidays_Name}"
                           FontSize="11"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource MutedTextBrush}"
                           VerticalAlignment="Center"
                           Background="Transparent"
                           Cursor="IBeam"
                           ToolTip="{DynamicResource Tip_Rename}"
                           MouseLeftButtonDown="TitleText_MouseLeftButtonDown">
                    <TextBlock.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="{DynamicResource Menu_Rename}" Click="TitleRenameMenuItem_Click">
                                <MenuItem.Icon>
                                    <Viewbox Width="16" Height="16" Margin="0,0,10,0">
                                        <Path Data="M20.71,7.04C21.1,6.65 21.1,6 20.71,5.63L18.37,3.29C18,2.9 17.35,2.9 16.96,3.29L15.12,5.12L18.87,8.87M3,17.25V21H6.75L17.81,9.93L14.06,6.18L3,17.25Z"
                                              Fill="{DynamicResource PrimaryTextBrush}"/>
                                    </Viewbox>
                                </MenuItem.Icon>
                            </MenuItem>
                        </ContextMenu>
                    </TextBlock.ContextMenu>
                </TextBlock>
                <TextBox x:Name="TitleEdit"
                         Visibility="Collapsed"
                         FontSize="11"
                         FontWeight="SemiBold"
                         Background="{DynamicResource ElevatedSurfaceBrush}"
                         Foreground="{DynamicResource PrimaryTextBrush}"
                         CaretBrush="{DynamicResource PrimaryTextBrush}"
                         SelectionBrush="{DynamicResource AccentBrush}"
                         BorderBrush="{DynamicResource AccentBrush}"
                         BorderThickness="1"
                         Padding="4,1"
                         MinWidth="100"
                         VerticalAlignment="Center"
                         HorizontalAlignment="Left"
                         PreviewKeyDown="TitleEdit_PreviewKeyDown"
                         LostFocus="TitleEdit_LostFocus"/>
            </Grid>

            <!-- ===== Content (populated in code: ВЧОРА -> СЬОГОДНІ -> ЗАВТРА; empty days hidden) ===== -->
            <Border Grid.Row="1"
                    Background="{DynamicResource ElevatedSurfaceBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="10,8,10,9">
                <StackPanel x:Name="ContentHost"/>
            </Border>
        </Grid>
    </Border>
</UserControl>
